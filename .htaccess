RewriteEngine on

# Stop hotlinking.
RewriteCond %{HTTP_REFERER} !^$
RewriteCond %{HTTP_REFERER} ^https?://([^/]+)/ [NC]
RewriteCond %1#%{HTTP_HOST} !^(.+)#\1$
RewriteRule \.(png|gif|svg)$ - [NC,F,L]

# Redirect /index.html to just the directory
RewriteCond
RewriteRule (.+)/index.html?$ /$1/ [R=301,NC,L]

# Prevent viewing of .htaccess file
<Files .htaccess>
order allow,deny
deny from all
</Files>

# Prevent directory listings
Options All -Indexes

